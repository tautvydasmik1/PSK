name: CI (tests + smoke)

on: [push, pull_request]

# run every shell step inside the Books/ folder where pom.xml lives
defaults:
  run:
    working-directory: Books

jobs:
  test-and-smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'maven'

      - name: Run unit tests (JUnit + JaCoCo)
        run: mvn -B -q -DskipTests=false test

      - name: Package app
        run: mvn -B -q -DskipTests package

      - name: Start app
        run: |
          nohup java -jar target/*.jar > app.log 2>&1 &
          echo $! > app.pid
          # wait for port 8080 to respond (up to ~90s)
          for i in {1..90}; do
            code=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/ || true)
            if [ "$code" != "000" ]; then
              echo "App is responding (HTTP $code)"; break
            fi
            echo "Waiting for app... ($i)"
            sleep 1
          done

      - name: Install jq (for JSON parsing)
        run: sudo apt-get update && sudo apt-get install -y jq

      # --- SMOKE #1: login-smoke must return 200 and a token (or set dummy) ---
      - name: Smoke 1/3 — login-smoke expects 200 + token
        run: |
          set -e
          HTTP_CODE=$(curl -s -o response.json -w "%{http_code}" \
            -H "Content-Type: application/json" \
            -d '{"username":"user1","password":"pass1"}' \
            http://localhost:8080/auth/login-smoke)   # NOTE: no /api prefix unless YOUR app uses it
          echo "Login HTTP code: $HTTP_CODE"
          [ "$HTTP_CODE" = "200" ]
          TOKEN=$(jq -r '.token // empty' response.json)
          if [ -z "$TOKEN" ]; then TOKEN="dummy-jwt-token"; fi
          echo "TOKEN=$TOKEN" >> $GITHUB_ENV

      # --- SMOKE #2 and #3: adjust paths if your app really uses /api/... ---
      - name: Smoke 2/3 — reservations expects 201
        run: |
          set -e
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Authorization: Bearer $TOKEN" \
            -H "Content-Type: application/json" \
            -d '{"bookId":1,"until":"2025-10-10"}' \
            http://localhost:8080/api/smoke/reservations)
          echo "Reservations HTTP code: $HTTP_CODE"
          [ "$HTTP_CODE" = "201" ]

      - name: Smoke 3/3 — comments expects 201
        run: |
          set -e
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Authorization: Bearer $TOKEN" \
            -H "Content-Type: application/json" \
            -d '{"bookId":1,"text":"Puiki!"}' \
            http://localhost:8080/api/smoke/comments)
          echo "Comments HTTP code: $HTTP_CODE"
          [ "$HTTP_CODE" = "201" ]

      - name: Stop app
        if: always()
        run: |
          if [ -f app.pid ]; then kill "$(cat app.pid)" || true; fi

      - name: Upload logs (for debugging)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-logs
          path: |
            Books/app.log
            Books/response.json
            Books/target/surefire-reports
